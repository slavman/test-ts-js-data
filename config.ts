const config = {
  client: 'mysql',
  connection: {
    host: 'localhost',
    user: 'newuser',
    password: 'password',
    database: 'posts',
    port: '3306'
  },
  table: 'post'
};

export default config;
